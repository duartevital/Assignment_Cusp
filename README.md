# Assignment Cusp

## Running the project

### Backend (.NET 10)

```bash
cd backend
dotnet run
```

### Frontend (React + Vite)

```bash
cd frontend
npm install   # first time only
npm run dev
```

The frontend dev server runs at `http://localhost:5173` by default.
